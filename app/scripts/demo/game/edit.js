//CKEDITOR.config.extraPlugins = 'print';

//CKEDITOR.replace( 'content' );

/* function updateCertInfo(key, value) {
 var str = CKEDITOR.instances.content.getData();
 
 if (str) {
 var st = $(str);
 var x = $(st).find('.cert_no').text(value);
 var processed_data = $(st).html();
 
 // CKEDITOR.instances.content.setData('');
 // CKEDITOR.instances.content.setEmpty();
 console.log(processed_data);
 // CKEDITOR.instances.content.insertHtml(processed_data);
 }
 $(key).text(value);
 
 } */

